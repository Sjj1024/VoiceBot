# 前端：填写后走「录音 → 自建 Whisper」，不再使用浏览器 Web Speech（无需 VPN）
VITE_WHISPER_URL=http://127.0.0.1:8787

# 前端：TTS 播报（走本地语音服务代理，不在前端暴露 Key）
# - 不填则默认用 /speech-api/tts（vite 已做 proxy）
VITE_TTS_URL=
VITE_TTS_VOICE=Cherry

# 本地 openclaw（OpenAI 兼容）接口
# - 常见：http://127.0.0.1:8000 （会请求 /v1/chat/completions）
VITE_OPENCLAW_URL=http://127.0.0.1:8000
# 如需鉴权再填（没有可留空）
VITE_OPENCLAW_API_KEY=
# 你的 openclaw 模型名（不确定可先留默认）
VITE_OPENCLAW_MODEL=
