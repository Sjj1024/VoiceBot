# 前端：填写后走「录音 → HTTP 转写」，不再使用浏览器 Web Speech（无需 VPN）
# - 返回 JSON：{"language":"Chinese","text":"你好呀"}
# - 开发时推荐 /speech-api：vite 已把 /speech-api 代理到 http://192.168.31.108:8000
# - 也可直填 http://192.168.31.108:8000（需服务端允许 CORS）
VITE_WHISPER_URL=/speech-api
# 若后端接收字段不是 audio，改为 file 等
# VITE_WHISPER_FORM_FIELD=audio
# 若转写路径不是 /transcribe，再改
# VITE_WHISPER_TRANSCRIBE_PATH=/transcribe

# 前端：TTS 播报（走本地语音服务代理，不在前端暴露 Key）
# - 不填则默认用 /speech-api（vite 已代理到语音服务；音色列表 GET /tts/voices）
VITE_TTS_URL=
VITE_TTS_VOICE=Cherry
# 音色列表路径（默认 /tts/voices）
# VITE_TTS_VOICES_PATH=/tts/voices

# 本地 openclaw（OpenAI 兼容）接口
# - 常见：http://127.0.0.1:8000 （会请求 /v1/chat/completions）
VITE_OPENCLAW_URL=http://127.0.0.1:8000
# 如需鉴权再填（没有可留空）
VITE_OPENCLAW_API_KEY=
# 你的 openclaw 模型名（不确定可先留默认）
VITE_OPENCLAW_MODEL=
